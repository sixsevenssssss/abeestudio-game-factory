# BACKLOG — abeeStudio · Инструменты

Один пункт = один тик (~30 мин). Берём сверху вниз, по одному.

## Фаза 1: Статические проверки (без браузера)

- [x] **Тик 0** — Первый тик: структура памяти проекта (PLAN, BACKLOG, STATE, LOG, API.md), зафиксировать в репо. Код не писать.
- [x] **Тик 1** — `src/serve.js` — локальный dev-сервер: Node.js http.createServer без внешних зависимостей, раздаёт статику, watch + Server-Sent Events для авто-перезагрузки, флаги --mobile (390×844 viewport), --throttle N (задержка мс), --open, --port N. 24/24 теста зелёные.
- [ ] **Тик 2** — `src/checks/runner.js` — инфраструктура чек-листа: декларативный реестр `checks[]`, каждый чек `{ id, title, fn(gameDir) → { status, message, fix? } }`, runner запускает все, форматирует отчёт (вердикт первым, детали после), выходной код 1 при fail. `src/check.js` — точка входа, парсит аргументы (--only=id, --json). Тест-фикстура `test/fixtures/minimal-game/` с минимальной структурой.
- [ ] **Тик 3** — `src/checks/i18n.js` — статическая проверка локализации: (L1) ключи ru.json vs en.json симметрия, (L2) нет пустых строк, (L3) сканировать src/ на строковые литералы > 3 символов вне L10n.t() — regex по .js файлам, (L4) language-screen.js или LanguageScreen в src/. Тест-фикстура с 4 поломками по одной на каждый подпункт, проверить что каждая ловится.
- [ ] **Тик 4** — `src/checks/platform.js` — статическая проверка платформенной чистоты: (P1) нет `ysdk.` вне `src/platform/`, (P2) нет `localStorage.` вне файлов с 'save' в имени и вне engine/, (P3) нет `href` с внешним доменом вне platform/. Тест-фикстура с 3 поломками.
- [ ] **Тик 5** — `src/checks/archive.js` — статическая проверка архива: (A1) index.html существует в корне, (A2) нет файлов с пробелами или кириллицей в именах (рекурсивно, кроме i18n/*.json и README), (A3) реальный вес всех файлов через fs.stat рекурсивно + топ-5 тяжёлых (не выдумывать, только измерять). Тест-фикстура с 3 поломками.
- [ ] **Тик 6** — `src/checks/magic-numbers.js` — магические числа: сканировать src/game/**/*.js (не src/game/data/) на числовые литералы кроме 0, 1, -1, 100, 1000 — если 5+ таких чисел = fail с перечислением мест. Тест-фикстура с поломкой.
- [ ] **Тик 7** — `src/checks/brand.js` — бренд: (B1) вызов Brand.showSplash() или showSplash в src/, (B2) строка 'abeeStudio' в src/screens/ или game/, (B3) about-screen.js или AboutScreen существует. Тест-фикстура с 3 поломками.
- [ ] **Тик 8** — `src/checks/ads.js` — честность рекламы: (AD1) каждый вызов `Ads.rewarded(` должен иметь строку с описанием награды в радиусе 5 строк (комментарий или строковый аргумент). AD2 — паттерн pay-to-win (одинокий rewarded без альтернативного пути) — статически сложно определить, ставить skip с объяснением. Тест-фикстура с поломкой AD1.
- [ ] **Тик 9** — `src/checks/store.js` — карточка: (S1) store/texts.ru.md и store/texts.en.md существуют и имеют секции «Название», «Короткое описание», «Описание», «Как играть»; валидация длин (название ≤50, короткое ≤70, описание 100–1000). (S2) store/icon-512.png, store/cover-800x470.ru.png или .en.png существуют. (S3) store/screens/ ≥ 2 файла. Тест-фикстура с 4 поломками.

## Фаза 2: Упаковка и тесты (без браузера)

- [ ] **Тик 10** — `src/pack.js` — упаковщик zip: рекурсивно обходит gameDir, исключает tools/, .git/, node_modules/, *.map, *.test.js, *.md (кроме LICENSE), STATE/BACKLOG/LOG/PLAN/API .md. Использует Node.js zlib или archiver (одна зависимость ок). Отчёт: вес .zip, разбивка по категориям, топ-10 тяжёлых. Проверяет index.html в корне zip. Реальный тест: создать zip, распаковать, проверить index.html на месте.
- [ ] **Тик 11** — `src/test.js` — тест-раннер: запускает test/**/*.test.js через `node --test` (v18+), если в monorepo и есть packages/engine/test/ — тоже. Отчёт: пройдено/упало/пропущено, список упавших с сообщением.
- [ ] **Тик 12** — `src/sim.js` — симулятор экономики: читает game.config.js (поля economy.*), --hours N (default 8, max 168), ASCII-кривая 20×60, детектирует дефицит монет и потолок. Тест с синтетическим конфигом: баланс без дефицита проходит, баланс с дефицитом через 2ч — warn.

## Фаза 3: Интеграция с шаблоном (разблокируется после engine тик 16)

- [ ] **Тик 13** — Запустить все статические чеки против реального template/. Исправить ложные срабатывания. Дозаполнить store/ в шаблоне. Обновить STATE.md.

## Фаза 4: Браузерные проверки (разблокируется после установки Puppeteer)

- [ ] **Тик 14** — Установить Puppeteer, проверить что запускается в окружении. Если не получилось — задокументировать причину в STATE.md. НЕ отмечать зелёным пока не проверено.
- [ ] **Тик 15** — `src/checks/network.js` — ноль внешних запросов: Puppeteer page.on('request'), фильтровать разрешённые домены, всё остальное = fail.
- [ ] **Тик 16** — `src/checks/console.js` — нет ошибок консоли: page.on('pageerror') + page.on('console') level error, прогон menu → gameplay → pause → settings.
- [ ] **Тик 17** — `src/checks/ads-runtime.js` — пауза на рекламе: эмулировать game_api_pause/resume, проверить Audio.duckForAd и Engine.loop.pause через window.__checks флаг.
- [ ] **Тик 18** — `src/checks/visibility.js` — сворачивание: dispatchEvent visibilitychange hidden, проверить мут и паузу.
- [ ] **Тик 19** — `src/checks/save-runtime.js` — сохранение: сыграть → перезагрузить → проверить прогресс. Эмулировать битый localStorage → игра не крашится.

## Фаза 5: Скриншоты и витрина

- [ ] **Тик 20** — `src/shots.js` — скриншоты через Puppeteer: menu screen в ru/en и portrait/landscape, сохранить в store/screens/. Требует браузер.
- [ ] **Тик 21** — `src/shots-resize.js` — нарезка размеров: из cover-800x470 нарезать все форматы площадок через sharp или jimp. Без нативных зависимостей если возможно.
- [ ] **Тик 22** — `src/video-record.js` — запись тиктоков: MediaRecorder/captureStream → webm → mp4 через ffmpeg. Если ffmpeg нет — skip с объяснением.

## Финал

- [ ] **Тик 23** — API.md финализация. Полный сквозной тест: generate → check → pack → убедиться что поломки ловятся. Обновить docs/API.md раздел «Tools».
