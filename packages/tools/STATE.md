# STATE — abeeStudio · Инструменты

## Статус: сделано 7 тиков из 24

### Готово
- [x] Тик 0 — структура памяти: PLAN, BACKLOG, STATE, LOG, API
- [x] Тик 1 — `src/serve.js` — dev-сервер (`npm start`), 24 теста
- [x] Тик 2 — `src/checks/runner.js` + `src/check.js` — каркас чек-листа, 28 тестов
- [x] Тик 3 — `src/checks/i18n.js` — L1–L4, 24 теста
- [x] Тик 4 — `src/checks/platform.js` — P1–P4, 30 тестов
- [x] Тик 5 — `src/checks/archive.js` — A1–A4, 21 тест
- [x] Тик 6 — `src/checks/magic-numbers.js` — M1, 18 тестов

### Что реально работает
- `npm start` — полностью.
- `npm run check` — 13 проверок в реестре:
  - A1 index.html в корне
  - A2 имена файлов без пробелов и не-ASCII
  - A3 распакованный вес + топ-5 тяжёлых (реальное измерение, видно в отчёте)
  - A4 время до первого кадра — ВСЕГДА skip: нужен браузер
  - L1 паритет ключей ru.json/en.json
  - L2 нет пустых значений
  - L3 нет текста, зашитого в код игры
  - L4 экран выбора языка присутствует
  - M1 нет магических чисел в логике игры (порог 5 разных значений)
  - P1 нет ysdk вне src/platform/
  - P2 нет localStorage мимо Save
  - P3 нет внешних адресов в коде и разметке (п. 8.4)
  - P4 нет постоянных соединений (WebSocket/EventSource/sendBeacon/XHR)

### Не сделано (17 тиков)
- Тики 7–9: бренд, честность рекламы, карточка
- Тик 10: `npm run pack` — упаковщик zip
- Тик 11: `npm test` — тест-раннер
- Тик 12: `npm run sim` — симулятор экономики
- Тик 13: интеграция с template/ (заблокирована: engine ещё не выпустил шаблон)
- Тики 14–19: браузерные проверки
- Тики 20–22: скриншоты, нарезка баннеров, запись роликов
- Тик 23: финальная сквозная проверка

### Критерии готовности (раздел 4 промпта)
1. Все команды на свежем проекте из шаблона — ❌ 2 команды из 6, шаблона ещё нет
2. `check` ловит подложенные поломки — ⚠️ для всех 13 реализованных проверено
   «от противного», каждая ловит свою. Ещё 9 проверок не написаны.
3. `pack` даёт рабочий архив — ❌ не написан
4. `API.md` описывает команды и коды возврата — ✅ (нереализованное помечено)

### Сломано
- (ничего)

### Починено
- Ложное зелёное при пустом чек-листе (тик 2) → «ВЕРДИКТ НЕ ВЫДАН», код 2.
- Ложное красное в L3: `'16px sans-serif'` считалось текстом для игрока (тик 3).
- A3 измеряла вес, но число не попадало в отчёт (тик 5) → флаг `show: true`.

### Проверено
- 145/145 тестов зелёные (Node.js v24)
- Каждая из 13 проверок проверена «от противного» на копии здоровой фикстуры:
  ловит свою поломку, называет `файл:строка`, объясняет чем чинить
- Сквозной прогон: игра с семью поломками разных типов (нет index.html,
  кириллица+пробел в имени, пропавший ключ en.json, текст в коде,
  localStorage, внешний fetch, баланс в логике) → ровно семь fail, exit 1
- Ложные срабатывания отдельно проверены и дают ok: `L10n.t`, комментарии,
  `console.warn`, CSS-значения, селекторы, имена событий, `src/engine/`,
  `ysdk` в адаптере, SDK-тег, `xmlns` SVG, `localhost`, `Save.set`,
  локальный `fetch`, имена `ru.json`/`en.json`, плохое имя внутри `tools/`,
  а для M1 — деление вдвое, проценты, миллисекунды, индексы массивов, hex,
  `Math.PI * 2`, `toFixed(2)`, `player2`, числа в `src/game/data/`
- `maskSource`: комментарии и содержимое строк заглушаются, номера строк
  и длина сохраняются (иначе отчёт соврал бы о месте)
- Отчёты прочитаны глазами на здоровой и на сломанной игре

### Честные границы реализованного
- A3 меряет РАСПАКОВАННЫЙ вес папки, не архив. Вес zip будет меряться `pack` (тик 10).
- A4 «время до первого кадра» — не измеряется вообще, всегда skip.
- P3/P4 статические: видят литералы в коде и разметке. Адрес, собранный в рантайме,
  увидит только браузерная N1 (тик 15). Зелёное P3 ≠ «сеть проверена целиком».
- Браузерный слой не начат: Puppeteer в этой песочнице не установлен и не пробовался.

### Замечено попутно
- `node --test test/` в Node 24 НЕ работает (трактует папку как модуль).
  Нужен glob `node --test "test/*.test.js"`. Учесть в тике 11.

### Состояние соседних сессий (04.08.2026 00:25 UTC)
- Фундамент: тик 12+ (DailySystem и далее), идёт с большим опережением.
- Интерфейс: тик 7+ (Toggle и далее), Playwright у них работает.
- template/: только README.md. Фаза 3 tools остаётся заблокированной.

### Следующий пункт бэклога
- Тик 7: `src/checks/brand.js` — B1–B3 (заставка abeeStudio, подпись студии, окно «Об игре»)
