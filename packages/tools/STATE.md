# STATE — abeeStudio · Инструменты

## Статус: сделано 4 тика из 24

### Готово
- [x] Тик 0 — структура памяти: PLAN, BACKLOG, STATE, LOG, API
- [x] Тик 1 — `src/serve.js` — dev-сервер (`npm start`), 24 теста
- [x] Тик 2 — `src/checks/runner.js` + `src/check.js` — каркас чек-листа, 28 тестов
- [x] Тик 3 — `src/checks/i18n.js` + `src/checks/util.js` — L1–L4, 24 теста «от противного»

### Что реально работает
- `npm start` — полностью.
- `npm run check` — работает, в реестре 4 проверки из 22 запланированных:
  - L1 паритет ключей ru.json/en.json
  - L2 нет пустых значений
  - L3 нет текста, зашитого в код игры
  - L4 экран выбора языка присутствует

### Не сделано (20 тиков)
- Тики 4–9: платформенная чистота, архив, магические числа, бренд, реклама, карточка
- Тик 10: `npm run pack` — упаковщик zip
- Тик 11: `npm test` — тест-раннер
- Тик 12: `npm run sim` — симулятор экономики
- Тик 13: интеграция с template/ (заблокирована: engine ещё не выпустил шаблон)
- Тики 14–19: браузерные проверки
- Тики 20–22: скриншоты, нарезка баннеров, запись роликов
- Тик 23: финальная сквозная проверка

### Критерии готовности (раздел 4 промпта)
1. Все команды на свежем проекте из шаблона — ❌ 2 команды из 6, шаблона ещё нет
2. `check` ловит подложенные поломки — ⚠️ частично: для L1–L4 проверено «от противного»,
   каждая поломка ловится своей проверкой. Остальные 18 проверок не написаны.
3. `pack` даёт рабочий архив — ❌ не написан
4. `API.md` описывает команды и коды возврата — ✅ (нереализованное помечено)

### Сломано
- (ничего)

### Починено
- Ложное зелёное при пустом чек-листе (тик 2): теперь пустой реестр и «все skip»
  дают «ВЕРДИКТ НЕ ВЫДАН» и код 2.
- Ложное красное в L3: `'16px sans-serif'` определялось как текст для игрока.
  Добавлен фильтр CSS-значений; кириллица при этом никогда не считается технической.

### Проверено
- 76/76 тестов зелёные (Node.js v24)
- Проверка «от противного» для L1–L4 проведена на копии фикстуры:
  - убран ключ из en.json → L1 fail, ключ назван ✓
  - убран ключ из ru.json → L1 fail, направление верное ✓
  - лишний ключ, нет файла, нет папки, битый JSON → L1 fail ✓
  - пустая строка / пробелы / null → L2 fail, ключ назван ✓
  - русский текст в коде → L3 fail с файлом и номером строки ✓
  - английская фраза в коде → L3 fail ✓
  - удалён language-screen.js → L4 fail ✓
  - четыре поломки разом → ровно четыре fail, каждая своя ✓
- Ложные срабатывания проверены отдельно: L10n.t, комментарии, console.warn,
  CSS-значения, селекторы, имена событий, код в src/engine/ — всё ok ✓
- Отчёт на сломанном проекте прочитан глазами: вердикт сверху, у каждой поломки
  место и объяснение человеческим языком.

### Браузерные проверки
- НЕ проверено: Puppeteer не установлен, в этой песочнице не пробовали. Тик 14.

### Замечено попутно
- `node --test test/` в Node 24 НЕ работает (трактует папку как модуль).
  Нужен glob `node --test "test/*.test.js"` или явный список файлов. Учесть в тике 11.

### Состояние соседних сессий (03.08.2026 21:50 UTC)
- Фундамент: тик 9 — AudioSystem. Идёт с большим опережением.
- Интерфейс: тик 6 — Button с ad-reward, Playwright у них работает.
- template/: только README.md. Фаза 3 tools остаётся заблокированной.

### Следующий пункт бэклога
- Тик 4: `src/checks/platform.js` — P1–P3 (нет ysdk вне platform/, нет localStorage мимо Save,
  нет внешних ссылок)
