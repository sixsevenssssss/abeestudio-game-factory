# STATE — abeeStudio · Инструменты

## Статус: сделано 5 тиков из 24

### Готово
- [x] Тик 0 — структура памяти: PLAN, BACKLOG, STATE, LOG, API
- [x] Тик 1 — `src/serve.js` — dev-сервер (`npm start`), 24 теста
- [x] Тик 2 — `src/checks/runner.js` + `src/check.js` — каркас чек-листа, 28 тестов
- [x] Тик 3 — `src/checks/i18n.js` — L1–L4, 24 теста «от противного»
- [x] Тик 4 — `src/checks/platform.js` — P1–P4, 30 тестов «от противного»

### Что реально работает
- `npm start` — полностью.
- `npm run check` — 8 проверок из 22 запланированных:
  - L1 паритет ключей ru.json/en.json
  - L2 нет пустых значений
  - L3 нет текста, зашитого в код игры
  - L4 экран выбора языка присутствует
  - P1 нет ysdk вне src/platform/
  - P2 нет localStorage мимо Save
  - P3 нет внешних адресов в коде и разметке (п. 8.4, включая CDN-шрифты)
  - P4 нет постоянных соединений (WebSocket/EventSource/sendBeacon/XHR)

### Не сделано (19 тиков)
- Тики 5–9: архив, магические числа, бренд, честность рекламы, карточка
- Тик 10: `npm run pack` — упаковщик zip
- Тик 11: `npm test` — тест-раннер
- Тик 12: `npm run sim` — симулятор экономики
- Тик 13: интеграция с template/ (заблокирована: engine ещё не выпустил шаблон)
- Тики 14–19: браузерные проверки
- Тики 20–22: скриншоты, нарезка баннеров, запись роликов
- Тик 23: финальная сквозная проверка

### Критерии готовности (раздел 4 промпта)
1. Все команды на свежем проекте из шаблона — ❌ 2 команды из 6, шаблона ещё нет
2. `check` ловит подложенные поломки — ⚠️ для L1–L4 и P1–P4 проверено «от противного»,
   каждая ловит свою. Остальные 14 проверок не написаны.
3. `pack` даёт рабочий архив — ❌ не написан
4. `API.md` описывает команды и коды возврата — ✅ (нереализованное помечено)

### Сломано
- (ничего)

### Починено
- Ложное зелёное при пустом чек-листе (тик 2) → «ВЕРДИКТ НЕ ВЫДАН», код 2.
- Ложное красное в L3: `'16px sans-serif'` считалось текстом для игрока (тик 3).

### Проверено
- 106/106 тестов зелёные (Node.js v24)
- Проверка «от противного» на копии здоровой фикстуры, каждая поломка ловится
  своей проверкой и называет `файл:строка`:
  - i18n: пропавший ключ (в обе стороны), лишний ключ, нет файла, нет папки,
    битый JSON, пустая строка / пробелы / null, русский текст в коде,
    английская фраза в коде, удалённый экран языка
  - platform: `ysdk.adv` в коде игры, `YaGames.init`, `localStorage.setItem`,
    `sessionStorage`, внешний `fetch`, внешняя ссылка в index.html,
    CDN-шрифт в разметке, `new WebSocket`, `sendBeacon`, `XMLHttpRequest`
  - Восемь поломок разом → ровно восемь fail, без перекрёстных срабатываний
- Ложные срабатывания проверены отдельно и дают ok: `L10n.t`, комментарии,
  `console.warn`, CSS-значения, селекторы, имена событий, код в `src/engine/`,
  `ysdk` внутри `src/platform/`, SDK-тег в index.html, `xmlns` SVG,
  `localhost`, `Save.set`, локальный `fetch` ассета
- `maskSource` протестирован отдельно: комментарии и содержимое строк
  заглушаются, номера строк и длина сохраняются (иначе отчёт соврёт о месте)
- Отчёт на сломанном проекте прочитан глазами: вердикт первой строкой,
  у каждой поломки место, причина и чем заменить

### Браузерные проверки
- НЕ проверено: Puppeteer не установлен, в этой песочнице не пробовали. Тик 14.
- P3/P4 ловят внешние адреса и соединения СТАТИЧЕСКИ (литералы в коде и разметке).
  Адрес, собранный в рантайме из частей, статикой не виден — это работа N1 (тик 15).

### Замечено попутно
- `node --test test/` в Node 24 НЕ работает (трактует папку как модуль).
  Нужен glob `node --test "test/*.test.js"`. Учесть в тике 11.

### Состояние соседних сессий (03.08.2026 22:50 UTC)
- Фундамент: тик 12 — DailySystem. Идёт с большим опережением.
- Интерфейс: тик 7 — Toggle.
- template/: только README.md. Фаза 3 tools остаётся заблокированной.

### Следующий пункт бэклога
- Тик 5: `src/checks/archive.js` — A1–A3 (index.html в корне, имена файлов без
  пробелов и кириллицы, реальный вес и топ тяжёлых файлов)
