# STATE — abeeStudio · Инструменты

## Статус: сделано 3 тика из 24. Пакет НЕ готов к использованию играми.

### Готово
- [x] Тик 0 — структура памяти: PLAN, BACKLOG, STATE, LOG, API
- [x] Тик 1 — `src/serve.js` — dev-сервер (`npm start`), 24 теста
- [x] Тик 2 — `src/checks/runner.js` + `src/check.js` — каркас чек-листа, 28 тестов

### Что реально работает
- `npm start` — полностью.
- `npm run check` — работает КАРКАС, но реестр проверок ПУСТ: ни одна из 22 запланированных
  проверок не написана. Команда честно отвечает «ВЕРДИКТ НЕ ВЫДАН» и кодом 2.

### Не сделано (21 тик)
- Тики 3–9: все статические проверки (i18n, платформа, архив, магические числа, бренд, реклама, карточка)
- Тик 10: `npm run pack` — упаковщик zip
- Тик 11: `npm test` — тест-раннер
- Тик 12: `npm run sim` — симулятор экономики
- Тик 13: интеграция с template/ (заблокирована: engine ещё не выпустил шаблон)
- Тики 14–19: браузерные проверки (сеть, консоль, пауза на рекламе, видимость, сохранения)
- Тики 20–22: скриншоты, нарезка баннеров, запись роликов
- Тик 23: финальная сквозная проверка

### Критерии готовности (раздел 4 промпта) — ни один не выполнен
1. Все команды на свежем проекте из шаблона — ❌ реализована 1 команда из 6, шаблона ещё нет
2. `check` ловит подложенные поломки — ❌ проверок нет, ловить нечем
3. `pack` даёт рабочий архив — ❌ не написан
4. `API.md` описывает команды и коды возврата — ✅ описывает, включая нереализованные (помечены)

### Сломано
- (ничего)

### Починено
- Ложное зелёное при пустом чек-листе: `check` выдавал «ГОТОВО К ОТПРАВКЕ» и код 0,
  не проверив ничего. Теперь пустой реестр и «все skip» → «ВЕРДИКТ НЕ ВЫДАН», код 2.

### Проверено
- 52/52 теста зелёные (Node.js v24, `node --test`)
- serve.js: раздача статики, Content-Type, 404, path traversal → 403, SSE
- check.js: пустой реестр → «ВЕРДИКТ НЕ ВЫДАН», exit 2 (проверено вручную)
- Проверка «от противного» для самих поломок игры — НЕ ПРОВОДИЛАСЬ: проверок ещё нет,
  ломать нечего. Появится начиная с тика 3.

### Браузерные проверки
- НЕ проверено: Puppeteer не установлен, в этой песочнице не пробовали. Тик 14.
- Наблюдение: сессия «Интерфейс» гоняет Playwright у себя успешно — значит headless-браузер
  в окружении фабрики в принципе поднимается.

### Состояние соседних сессий (03.08.2026 20:25 UTC)
- Фундамент: тик 7 — SaveSystem. Идёт с опережением, ~115 тестов.
- Интерфейс: тик 4 — showcase с Playwright.
- template/: только README.md. Фаза 3 tools остаётся заблокированной.

### Следующий пункт бэклога
- Тик 3: `src/checks/i18n.js` — первая реальная проверка (L1–L4)
