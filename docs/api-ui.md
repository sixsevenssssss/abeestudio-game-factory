# API: ui

Заполняет сессия «Интерфейс». Другие сессии этот файл только читают.

Формат записи для каждой единицы: что это, параметры, что возвращает, события, пример на три строки.

_Пока пусто — появится после первых тиков._
